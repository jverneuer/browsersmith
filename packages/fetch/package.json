{
  "name": "@browsercore/fetch",
  "version": "0.1.0",
  "description": "Developer-facing high-level HTTP API. Composes transport, tls, http1, http2, profiles, and cookies into a single fetch() surface.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "LICENSE", "README.md"],
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run clean && npm run build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "@browsercore/cookies": "^0.1.0",
    "@browsercore/http1": "^0.1.0",
    "@browsercore/http2": "^0.1.0",
    "@browsercore/profiles": "^0.1.0",
    "@browsercore/tls": "^0.1.0",
    "@browsercore/transport": "^0.1.0"
  }
}
